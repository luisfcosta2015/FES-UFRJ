vem do php:
um hash

imagine que precisamos do nome que nao vem como parametro

{
  "static_field":
    {
      "key": "nomeescola,addrescola",
      "value": "select nameescola,addrescola where escola.id=${idescola}"
    },
  "dynamic_field": {
    "key": "id,nome,situacao",
    "value": "select ..."
  }
}
